/**
 * Setup global para tests (Jest + React Native)
 * Para matchers extra (toBeOnTheScreen, etc.) instalar @testing-library/jest-native
 * Mocks de AsyncStorage/SecureStore están en __tests__/mocks.ts (setupFiles).
 */
